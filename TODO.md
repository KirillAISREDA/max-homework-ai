# TODO

Приоритеты сверху вниз внутри каждого блока. План по неделям — из `docs/architecture.md`, раздел 10.
Конкурсные дедлайны — из `docs/contest/contest.md` (Sber500xDisrupt).

## 🔴 КОНКУРС — дедлайны

- [x] Заявка подана 30.08.2026 (Кирилл)
- [x] Проект допущен к первому этапу Sber500xDisrupt (13.09.2026)
- [ ] К 15.10.2026: MVP в MAX + ≥30 клиентских интервью + **≥70 реальных пользователей** + аналитика + концепция вывода на рынок
- [ ] Зачётный период ~03.11–30.11: DAU ≥ 1000 устойчиво (23/28 дней), обращений ≥ 10/DAU (по методике конкурса = вызовы компонентов — наш пайплайн даёт десятки на домашку)
- [ ] С первого пользователя: лог каждого вызова компонента (trace_id, обезличенный user_id, тип вызова, результат, код ошибки) + разделение прод/тест трафика — под антифрод
- [ ] Реестр сторонних компонентов (библиотеки, модели, API) — п. 5.3 Положения

## 🔴 Баги из живых логов 06–13.09 (сессия 11): 15 проверок — 0 «верно»

- [x] `mathparse`: «:» заменялся на «/» текстом → `4/5 : 9/10` = 2/225 → ложная «ошибка» на верном делении дробей (сессия 11)
- [x] `mathparse._eval_segment`: `AttributeError` из `parse_expr` («40 . 40») ронял обработку всего фото (сессия 11)
- [x] Сопоставление тетрадь↔учебник: номер даёт баллы, только если записан на обеих страницах (`number_on_page` по транскрипции, сессия 11)
- [x] Строка-продолжение «= (-12)/2 + … = -10» пересчитывается от хвоста предыдущей строки (сессия 11)
- [x] Ответ фразой «Отв.: на 10 яблок надо 160»: метка «Отв.», пояснение после тире с числами, при непарсящемся ответе — сверка последней пересчитанной строки с эталоном (сессия 11)
- [ ] Задеплоить исправления на VPS и проверить на живом альбоме
- [ ] Сохранять фото пользователей (Object Storage/том, с TTL) — без них спорные OCR-случаи не перепроверить
- [ ] `merge_textbook`: страница учебника с придуманными номерами перезаписывает прошлые условия с теми же номерами (13.09 «1.124» затёрло «Отметьте точки»)
- [ ] В сводке «№1 — …» для придуманного номера: показывать «Задание 1», чтобы не путать ребёнка
- [ ] Покрытие валидатора: уравнения с x/y (корень подстановкой), сложение столбиком — сейчас всегда uncertain
- [ ] Vision: «·» читается как «:» (06.09, 08.09 — в учебнике «·», в транскрипции тетради «:»), цифры 850→860 — ложные ошибки от OCR
- [ ] Задание из нескольких пунктов (а–г) → один эталонный ответ → тьютор не может засчитать ответ ученика (06.09: 7 реплик, resolved так и не наступил)

## Новые предметы: русский, английский, окружающий мир (решение Кирилла, 13.09)

- [ ] Спроектировать предметы: что проверяется детерминированно (как SymPy для математики), что — только LLM; архитектура §3.3 сейчас заточена под математику
- [ ] Русский язык: проверить, не «исправляет» ли GigaChat vision орфографию ученика при транскрипции (для проверки диктанта это фатально) — замер на 10–20 фото с известными ошибками
- [ ] Оценить OCR ReadingPipeline (`ai-forever/ReadingPipeline-notebooks`, обучен на `school_notebooks_RU`) как посимвольное распознавание рукописи без автоисправления; языковую модель kenlm при этом отключать — она тоже тянет к «правильным» словам
- [ ] Выяснить, записан ли текст в разметке `school_notebooks_RU` как в тетради (с ошибками) — от этого зависит, годится ли датасет для замера CER на ошибках учеников
- [ ] Английский: `school_notebooks_EN` всего 90 фото — для оценки хватит, для дообучения нет

## Ближайшее (нужно от Кирилла)

- [x] Доступ к GigaChat API и аккаунт Cloud.ru Evolution — есть
- [x] Креды в `.env` (перс-аккаунт, PERS; сертификат Минцифры в `certs/`), `hwcheck ping` — ОК (0.66 с)
- [x] Регистрация бота в MAX, токен в `.env` (`MAX_TOKEN`, `MAX_CA_BUNDLE`) — бот «Домашка ИИ» живой (сессия 9)
- [x] Логотип бота выбран — робот, `assets/branding/logo.svg` + `logo-500.png` (сессия 9)
- [ ] Залить в MasterBot аватар `assets/branding/logo-500.png` и описание бота (текст в HISTORY, сессия 9)
- [x] Датасет 31 фото собран, три прогона eval сделаны (см. HISTORY, сессия 3)
- [ ] От Кирилла: 2–3 фото В ОРИГИНАЛЕ с телефона (без пережатия мессенджером) — проверить гипотезу «дело в сжатии»
- [ ] Помнить: PERS-фримиум лимитирован по Pro/Max; уже сожгли ~1 млн токенов на 3 прогона. Для экспериментов дальше, вероятно, нужен B2B-пакет

## Приоритет: качество распознавания (главный риск, вскрыт 2026-08-31)

- [x] Гипотеза кропов — НЕ помогает; гипотеза «виноват JSON» — подтвердилась иначе: **двухэтапный vision (транскрипция → структурирование текстом) решил проблему: 28/31 и 4/4** (сессия 7)
- [x] Развилка А/Б закрыта: остаёмся на GigaChat, спецмодель OCR не нужна на MVP
- [x] Сценарий «фото учебника + фото тетради»: роли страниц, условия по номеру/по числам условия, проверка только тетради (сессия 9)
- [x] Dev-лог распознанной страницы; запас в правиле роли страницы (сессия 9, 05.09)
- [ ] Калибровка роли страницы и сопоставления на реальных фото по журналу `vision_recognized.role` и логу структуры
- [x] Вердикт «все строки верны, ответа нет»: значение последней строки сверяется с эталоном (совпало → correct, иначе uncertain) — сессия 11
- [ ] Промпт структуризации: геометрия/углы тоже задания (провал photo_14)
- [ ] Мини-разметка ground truth (5 фото) для символьной accuracy распознавания цифр
- [ ] Калибровка/недоверие к confidence модели (0.9 при неверном чтении)

## Неделя 2 — Solver + Validator (НЕ блокируется vision, вход текстовый)

- [x] Solver: решение по тексту условия, structured output, самопроверка эталона Validator'ом
- [x] Validator: детерминированный пересчёт шагов (SymPy, точные Rational), сверка ответов
- [x] Grade: вердикт correct/wrong/uncertain, первый ошибочный шаг, описки
- [x] Кэш решений файловый (sha256 model+prompt+task); Redis — при переезде в облако
- [x] CLI: `hwcheck solve` и `hwcheck grade`
- [ ] Function calling `calc()` в Solver — если эталоны начнут ошибаться на реальных заданиях
- [ ] Прогнать Solver по заданиям из размеченного датасета, посчитать долю верных эталонов
- [ ] Self-consistency (2 решения при расхождении) — арх. §3.3, после оценки качества эталонов

## Неделя 1 — фундамент и офлайн-оценка

- [x] Репозиторий, стартовые файлы (HISTORY, TODO, CLAUDE.md)
- [x] Скелет проекта: pyproject.toml (uv), ruff, mypy strict, pytest, CI (GitHub Actions)
- [x] Клиент GigaChat поверх официального SDK `gigachat` (OAuth внутри SDK)
- [x] Абстракции `LLMClient` / `VisionClient` (GigaChat — реализация №1)
- [x] Скрипт офлайн-оценки: латентность/токены/валидность JSON по датасету фото
- [ ] Замер латентности vision-этапа на реальных фото (нужны креды в .env + фото)
- [ ] Разметка ground truth в датасете и подсчёт accuracy распознавания (формат разметки — обсудить)
- [ ] Retry/backoff и circuit breaker для GigaChat (сейчас — только то, что даёт SDK)

## Неделя 2 — ядро пайплайна (CLI, без инфраструктуры)

- [ ] Vision/Parser: сегментация фото на задания, JSON с confidence
- [ ] Solver: независимое решение задания, structured output (pydantic)
- [ ] Validator: SymPy-пересчёт шагов, проверка единиц и знаков
- [ ] Кэш решений: hash(изображение + условие) → результат, Redis, TTL 30 дней
- [ ] Промпты v1 в `prompts/` с версионированием
- [ ] Пайплайн Vision → Solver → Validator как CLI-команда

## Неделя 3 — тьютор и генератор

- [x] Comparator + Error Classifier (таксономия, confidence<0.6 → unclear)
- [x] Tutor: FSM подсказок 0→3, ответ в промпт только на уровне 3, resolved по детерминированной сверке
- [x] Exercise Generator + валидация эталона Validator'ом (1 retry → отброс)
- [x] Всё в одном контейнере воркера (Dockerfile) — сессия 10

## Неделя 4 — интеграция с MAX

- [x] Клиент MAX Bot API (updates/messages/answers/download), толерантные модели апдейтов
- [x] FSM диалога за протоколом StateStore (in-memory; Redis — при деплое)
- [x] Сценарий: фото → vision → проверка по заданиям → кнопки «Разобрать» → тьютор
- [x] EventLog: конкурсный учёт вызовов компонентов, обезличенные id, dev/prod
- [x] Семафор GigaChat (фримиум 1 поток) — лечит 429
- [x] CLI `hwcheck bot` — long polling, без публичного URL
- [x] Живой тест с реальным ботом MAX: альбом учебник+тетрадь → №19 correct (сессия 9)
- [ ] Живой тест диалога тьютора (кнопка «Разобрать») и повторной ошибки
- [ ] Webhook-режим (FastAPI) + дедуп update_id — когда polling перестанет справляться (сейчас не нужен)
- [ ] PostgreSQL-схема (миграции), Object Storage для фото
- [x] Dockerfile + docker-compose, бот развёрнут в контейнере на VPS 193.247.73.243 (`docs/deploy.md`, сессия 10)
- [ ] Redis `StateStore` вместо in-memory: рестарт контейнера сбрасывает открытые диалоги тьютора
- [ ] `ENVIRONMENT=prod` на VPS перед первым реальным пользователем (сейчас dev — события не в зачёт)
- [ ] Переезд на Cloud.ru (Container Apps) — когда потребуется по условиям конкурса/нагрузке; VPS общий с другими проектами

## Неделя 5 — student model, отчёты, admin

- [ ] Student Model Service (rule-based scoring, контракт под будущий BKT)
- [ ] Report Scheduler (CronJob, воскресенье 18:00) + Parent Report
- [ ] Admin/Review UI (Streamlit за basic-auth): очередь спорных проверок
- [ ] Мониторинг: метрики пайплайна, токены/стоимость по шагам, hit rate кэша

## Неделя 6 — пилот

- [ ] Пилот 10–20 семей
- [ ] Нагрузочный тест k6 на 1000 DAU (синтетика с кэшированными ответами GigaChat)
- [ ] Проверка блокирующих показателей: accuracy ≥ 90 %, ложные «ошибки» ≤ 5 %
